import 'package:flutter/material.dart';

class RegistrationMockScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Mock Registration Screen"),
      ),
    );
  }
}
